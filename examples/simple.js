// [Migrated to TypeScript]
// A simple JS file for migration testing
function greet(name) {
    return "Hello, " + name;
}
var user = {
    id: 1,
    name: "Alice"
};
console.log(greet(user.name));
